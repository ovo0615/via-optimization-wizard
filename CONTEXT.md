# Via Optimization Wizard

把 optiSLang 的多目標最佳化包成一個四步精靈，讓第一次接觸的人在十幾分鐘內看懂「為什麼要用最佳化」，並且看得出結果是 optiSLang 算的。第一版鎖定 PCB 差分 via 的訊號完整性題目。

此工具由虎門科技資深技術工程師 Jeff Hong 洪敬傑提供。

## Language

### 模式

**展示模式**：
在客戶面前跑的模式，用內建範例、預設參數範圍與刻意調輕的求解設定。
_Avoid_: demo mode、簡易模式

**專案模式**：
工程師拿自己的疊構與 via 設定跑的模式，參數與範圍可自由指定。
_Avoid_: 進階模式、生產模式

### 建模

**模型描述**：
一份 JSON，完整描述疊構、padstack、via 擺放、出線與求解設定。整個 via 模型是這份 JSON 的純函數——同一份 JSON 一定重建出同一個模型。
_Avoid_: 專案檔、config、project.json

**重建**：
把改過數字的模型描述交給建模器，產生一個全新的 `.aedb`。因為幾何值是烘死的數字而非 AEDT 變數，每個設計點都必須重建，不能只改變數。
_Avoid_: 更新模型、改參數、re-parameterize

**建模器**：
吃模型描述、產出可求解 `.aedb` 的模組，由 Via Wizard 的 `modeling.py` fork 而來。
_Avoid_: modeling、EDB builder

### 最佳化

**設計變數**：
optiSLang 會去掃的幾何量。展示模式固定四個：antipad 直徑、差分 pitch、GND via 距離、backdrill stub。
_Avoid_: 參數、parameter、輸入

**響應**：
每個設計點回報給 optiSLang 的純量。三個：反射峰值（求解得來）、keep-out 面積（幾何算得來）、殘樁共振頻率（公式算得來）。三者的取得成本差三個數量級，這是刻意的。
_Avoid_: 輸出、結果、KPI

**反射峰值**：
via 段 TDR 曲線上 `|dZ/dx|` 的局部極大處，換算成的反射係數 `|Γ| ≈ ΔZ / 2Z`。是電性目標，最小化。用階梯而不是用「偏離目標阻抗」，因為造成反射的是阻抗的落差——整段 45Ω 的線幾乎不反射，50Ω 陡降到 45Ω 再陡升回來的凹陷才反射明顯。
_Avoid_: 阻抗偏差、Z 誤差、SDD11

**上升時間**：
TDR 的量測邊緣，由 Touchstone 頻寬導出：`Tr = 0.35 / f_max`。**不是使用者可填的欄位**，也不是驅動器規格——填快了是製造資料裡不存在的解析度，填慢了是浪費已經花錢求解的頻寬。
_Avoid_: rise time 設定、邊緣速率

**空間解析度**：
`v × Tr / 2`。TDR 能分辨的長度下限。因為 via 垂直長度只有 1～2 mm，這個量決定了掛頻上限必須開多高，是本專案風險最高的假設。
_Avoid_: 精度、解析力

**殘樁共振頻率**：
背鑽後剩下的開路殘樁造成的陷波頻率，`f = n × c / (4 × stub_mm × √Dk)`，奇次諧波都會出現。純公式算出來、零求解成本，在最佳化裡當約束（必須高於工作頻寬的設定倍數），在精靈第二步當即時顯示。
_Avoid_: 陷波、notch、諧振

**診斷輸出**：
給人看的、不進最佳化的有號資訊：阻抗往下凹代表電容性（建議放大 antipad 或縮小 pad），往上凸代表電感性（建議 GND via 拉近）。與最佳化用的反射峰值是兩回事——後者取絕對值，前者保留方向。
_Avoid_: 建議、提示、insight

**設計點**：
一組具體的設計變數值，以及它重建、求解、抽響應之後的完整結果。
_Avoid_: 樣本、case、run

**keep-out 面積**：
via 群佔掉的佈線面積，由 antipad、pitch 與 GND via 距離推算。它是最佳化的第二個目標，代表「佈線密度」這個與電性能對打的成本。
_Avoid_: 面積、佔位、footprint

**三段式流程**：
optiSLang 的固定流程：敏感度分析（ALHS 採樣）→ MOP（建代理模型）→ 最佳化。每一段都有一張圖可以對客戶講。
_Avoid_: workflow、pipeline、OCO

### 求解

**求解器**：
吃一組設計變數值、回傳響應的可換介面。目前有三種實作：HFSS batch（真求解）、預跑查表（備援）、假求解器（開發用解析函數）。SimAI 未來會是第四種。
_Avoid_: solver backend、engine

**假求解器**：
用解析函數在毫秒內回傳響應的實作，讓 optiSLang 那段可以脫離 HFSS 獨立開發與測試。
_Avoid_: mock、stub、dummy

**預跑資料庫**：
事先跑完的 optiSLang 專案，加上每個設計點的 Touchstone。現場沒時間或跑不動時的退路，也讓客戶能當場改目標權重重跑而不必再解 HFSS。
_Avoid_: 快取、cache、預算結果
